<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        margin: 40px auto;
        max-width: 650px;
        line-height: 1.6;
        font-size: 18px;
        font-family: "Courier New", Courier, monospace;
        color: #444;
        padding: 0 10px;
      }
      h2 {
        line-height: 1.2;
      }
    </style>
  </head>

  <body>
    <h2>Efflux Video Streaming</h2>
    <p>This video is 4MB and is being streamed instead of downloaded.</p>
    <p>
      Feel free to seek through the video and it only loads the part you want to
      watch
    </p>
    <h1><%= id %></h1>
    <h1><%= data["Title"] %></h1>
    <h2>Year: <%= data["Year"] %> Runtime: <%= data["Runtime"] %> </h2>
    <h2>Director:<%= data["Director"] %></h2>
    <img src="<%=data['Poster']%>" alt="">
    <p><%= data["Plot"] %></p>
    <video id="videoPlayer" width="650" controls muted="muted" autoplay>
      <source src="/efflux-api/<%= id %>"  type="video/mp4" />
    </video>
    <i>KDCloudy</i>
  </body>
</html>
